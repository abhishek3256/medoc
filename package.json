{
    "name": "medoc-hospital-app",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "install-all": "cd frontend && npm install && cd ../api && npm install",
        "build": "cd frontend && npm install && npm run build"
    },
    "engines": {
        "node": "18.x"
    }
}